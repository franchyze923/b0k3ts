apiVersion: v1
kind: Service
metadata:
  name: {{ include "b0k3ts.fullname" . }}
  labels:
    {{- include "b0k3ts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.ports.ui }}
      targetPort: http
      protocol: TCP
      name: http-ui
    - port: {{ .Values.service.ports.server }}
      targetPort: 8080
      protocol: TCP
      name: http-server
  selector:
    {{- include "b0k3ts.selectorLabels" . | nindent 4 }}
