
 Upload a named kubeconfig (saved in Badger)
Multipart

curl -X POST \
  -F "file=@</path/to/kubeconfig.yaml>" \
  "http://<host>:<port>/api/v1/kubernetes/kubeconfigs/dev"

  List OBCs (in-cluster)
  curl "http://<host>:<port>/api/v1/kubernetes/obc/<namespace>?mode=incluster"

  List OBCs (using uploaded kubeconfig
  curl "http://<host>:<port>/api/v1/kubernetes/obc/<namespace>?mode=kubeconfig&config=dev"

  Apply an OBC (Server-Side Apply)
  curl -X POST \
    "http://<host>:<port>/api/v1/kubernetes/obc/<namespace>/apply?mode=kubeconfig&config=dev" \
    -H "Content-Type: application/yaml" \
    --data-binary @</path/to/obc.yaml>

    curl -X DELETE \
      "http://<host>:<port>/api/v1/kubernetes/obc/<namespace>/<name>?mode=kubeconfig&config=dev"


### 6) Extract Secret creds for a bucket (secret name == bucket name)
curl \
  "http://<host>:<port>/api/v1/kubernetes/obc/<namespace>/my-first-bucket/secret?mode=kubeconfig&config=dev"



